# Copy this file to .env for local development.
# Do not commit real secrets or credentials.

APP_ENV=local
BACKEND_PORT=8080
FRONTEND_PORT=5173

# Placeholder only. Use a local mock agent during early development.
AGENT_PROVIDER=mock
AGENT_TIMEOUT_SECONDS=30

# Leave empty unless a future integration explicitly requires it.
LLM_API_KEY=
GITHUB_TOKEN=
