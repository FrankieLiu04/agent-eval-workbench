# EN: Spring does not load this file automatically; inject these values through your shell or IDE.
# CN: Spring 不会自动读取此文件；请通过 shell 或 IDE 注入这些变量。

ARTIFACT_ROOT=./data/artifacts
SERVER_ADDRESS=127.0.0.1
BENCHMARK_JOB_TIMEOUT_SECONDS=300
BENCHMARK_WORKER_LEASE_SECONDS=30
BENCHMARK_SWEEP_MS=5000

DATABASE_URL=jdbc:postgresql://localhost:5432/agent_workbench
DATABASE_USERNAME=agent_workbench
DATABASE_PASSWORD=
