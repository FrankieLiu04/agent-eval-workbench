spring:
  application:
    name: agent-eval-workbench
  profiles:
    default: local

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
