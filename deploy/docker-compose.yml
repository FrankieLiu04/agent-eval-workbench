services:
  backend:
    image: alpine:3.20
    command: ["sh", "-c", "echo backend placeholder; sleep 3600"]
    ports:
      - "${BACKEND_PORT:-8080}:8080"

  frontend:
    image: alpine:3.20
    command: ["sh", "-c", "echo frontend placeholder; sleep 3600"]
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
